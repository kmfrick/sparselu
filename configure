#!/bin/sh
# Configure script to ensure Rcpp attributes are generated before compilation.

echo "Running Rcpp::compileAttributes() ..."
"${R_HOME}/bin/Rscript" --vanilla -e "if (requireNamespace('Rcpp', quietly = TRUE)) Rcpp::compileAttributes()" || exit 1

exit 0
